version: "1.0"

project:
  language: go
  profile: generic
  build:
    cross_compile: true
    platforms:
      - linux-amd64
      - linux-arm64
      - darwin-amd64
      - darwin-arm64
      - windows-amd64
      - windows-arm64
  release:
    mode: direct
